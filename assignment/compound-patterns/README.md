## MVC

MVC has observer(m), strategy(c) and composite(v) desgin patterns.

- controller could be a **strategy**. Why strategy and not mediator? bc it's not a two way street
- MVP: model-view-presenter: presenter act as Mediator. But it dies already.
